export default function ErrorBoundary() {
  return (
    <section>
      <h1>Router Boundary Error</h1>
      <h3 style={{ color: 'red' }}>
        Don't refresh this search page instead select the item you need to search!
      </h3>
    </section>
  )
}
